# Linkable elements 

Creates a clickable element and onclick redirects the user to the specified data-url

## Usage

```javascript
$('.redirects').linkableObject()
```

```html
<div class="redirects" data-url="http://some-osom-url.com">Click here to be redirected</div>
```